/**
 * Spring configuration: the Mongo transaction manager and auditing, CORS, OpenAPI, static
 * resource handling, and the {@code @ConfigurationProperties} types that replace the
 * magic numbers previously inlined in services.
 *
 * @see com.dominator.gearly.platform
 */
package com.dominator.gearly.platform.config;
